package $javaModel.packageName;

#foreach ( $import in $javaModel.imports )
import $import
#end

$javaBody = $javaModel.body
#if ( $javaBody.isClass)   #* case of Class  *#
#else                      #* case of Interface  *#
#foreach( $modifier in $javaBody.modifiers ) $modifier  #end interface $javaBody.name { }
#end

package $packageName;
#foreach ( $import in $imports )
import $import;
#end